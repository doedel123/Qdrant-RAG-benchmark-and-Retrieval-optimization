# Produktions-Deps fuer FastAPI-Server.
# Lokaler Betrieb: mxbai-de Embedding + Cross-Encoder laufen ueber
# sentence-transformers, Query Expansion ueber Ollama HTTP API.

fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9

qdrant-client>=1.12
sentence-transformers>=2.2
requests>=2.32
numpy>=2.0
python-dotenv>=1.0
pyyaml>=6.0
